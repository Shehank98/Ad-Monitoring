diff --git a/.claude/agents/numbers-guardian.md b/.claude/agents/numbers-guardian.md
--- a/.claude/agents/numbers-guardian.md
+++ b/.claude/agents/numbers-guardian.md
@@ -15,8 +15,21 @@
 2. Engines are called with mode="smart" only; no reset, de-match or delete calls.
 3. Channel and month are never built from input or LLM output; always read from Schedule rows.
 4. Every LMRBRow candidate query excludes all four lock flags.
-5. Every write goes through gate.py and creates an AgentAction with before and after values.
+5. Every write to a core table goes through gate.perform and creates an AgentAction
+   with before and after values. For agent actors the gate re-checks AgentConfig.enabled
+   immediately before each write. For human actors (admin, human_confirmed=True) the
+   gate checks the admin role and the per-action rules instead of the kill switch.
+   Admin edits to AgentConfig, AgentAccountOverride and AllowedSender are logged and are
+   not blocked by the kill switch.
 6. Wildcard and pipe mapping values are never auto-applied.
 7. LLM output is validated, and IDs are restricted to the candidates supplied.
 8. Email and file content is wrapped as data and never executed as instructions.
+19. The intake LLM tool list contains only detect_tc, find_schedules, get_schedule,
+    check_brand_overlap and submit_decision. The final decision is computed in code,
+    and the LLM can only downgrade it.
+20. Cron-side intake code never imports default_storage and never writes
+    TransmissionReport, TCRow or any other core table.
+21. Data sent to the LLM is limited to what C5 allows: sender, subject, file name, the
+    body truncated to 4,000 characters, and tool results (no full file content; at most
+    20 sample themes), all inside <data> tags.
 Then run the existing test suite and agent_golden_check, and report the results.
