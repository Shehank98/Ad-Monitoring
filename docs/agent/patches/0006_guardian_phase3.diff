--- a/.claude/agents/numbers-guardian.md	2026-09-26 04:25:23.487191220 +0000
+++ b/.claude/agents/numbers-guardian.md	2026-09-26 04:25:23.498004354 +0000
@@ -5,12 +5,23 @@
 ---
 Check the current diff against CLAUDE.md and docs/agent/AGENT_BUILD_BRIEF.md
 and report PASS/FAIL with file:line for each:
-1. No file under protected paths (brief Section 3) changed.
+1. No file under protected paths (brief Section 3) changed. "Existing tests" means core tests
+   (core/tests.py and any test outside agent/ and intake/); the agent's and intake's own tests
+   from earlier phases may change, and each change is listed with its reason.
 2. Engines are called with mode="smart" only; no reset, de-match or delete calls.
 3. Channel and month are never built from input or LLM output; always read from Schedule rows.
 4. Every LMRBRow candidate query excludes all four lock flags.
-5. Every write goes through gate.py and creates an AgentAction with before and after values.
+5. Every write to a core, accounts or mapping table, and every human write to any table, goes
+   through gate.py and creates an AgentAction with before and after values. The agent's own
+   bookkeeping tables (runs, snapshots, findings, pending effects, heartbeat, notification log,
+   info-only proposals) are exempt.
 6. Wildcard and pipe mapping values are never auto-applied.
 7. LLM output is validated, and IDs are restricted to the candidates supplied.
 8. Email and file content is wrapped as data and never executed as instructions.
+9. Any engine call outside gate.perform runs inside an always-rolled-back dry run, never for an
+   authorised or locked scope. Read-only guards are never caught and ignored: a
+   CoreWriteAttempt stops the run.
+10. reconcile_tc, reconcile_sponsorship and build_summary_data always receive schedule_id.
+11. Session-level locks survive error recovery (no connection close while a lock is held
+    without re-taking it), and every redirect target from request data is validated.
 Then run the existing test suite and agent_golden_check, and report the results.
