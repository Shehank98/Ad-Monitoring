{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py fetch_tc_emails && python manage.py run_tc_intake_agent",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
